.env
*.env
prod.env
.env.local
.env.*.local
# Locally generated plaintext env material (secrets belong git-crypt-encrypted
# in .github/environments.json)
environments/

# This secret-bearing file is allowed in git only because .gitattributes
# stores it through git-crypt.
!.github/environments.json
.temp
backend/scripts/tmp-*.ts
backend/.tmp-db-seeds.secrets.json
.tmp-backend-prod.env

node_modules/
.next/
.vercel/
dist/
coverage/
*.tsbuildinfo

terraform/**/.terraform/
terraform/**/*.tfstate
terraform/**/*.tfstate.*
terraform/**/tfplan

.DS_Store
Thumbs.db

# expo CLI run at the repo root regenerates a stub app.json — never track it
/app.json
