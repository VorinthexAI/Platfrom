.env
*.env
prod.env
.env.local
.env.*.local
# Locally generated plaintext env material (secrets belong git-crypt-encrypted
# in .github/environments.json)
environments/

# These secret-bearing files are allowed in git only because .gitattributes
# stores them through git-crypt.
!launch/.env
!.github/environments.json
.temp
backend/scripts/tmp-*.ts
backend/.tmp-db-seeds.secrets.json
.tmp-backend-prod.env

node_modules/
.next/
.vercel/
dist/
coverage/
*.tsbuildinfo
graphify-out/

launch/**/outputs/*
!launch/**/outputs/.gitkeep

terraform/**/.terraform/
terraform/**/*.tfstate
terraform/**/*.tfstate.*
terraform/**/tfplan

.DS_Store
Thumbs.db

# Transient asset/gallery dev-server logs (lock git checkout on Windows)
scripts/image/assets-server.*.log

# expo CLI run at the repo root regenerates a stub app.json — never track it
/app.json
