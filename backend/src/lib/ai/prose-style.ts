export const USER_VISIBLE_AI_PROSE_POLICY = 'In newly authored user facing prose, refer to the platform only as Vorinthex AI, never as Vorinthex alone. Do not use hyphen or dash characters. Rewrite compound terms and sentence breaks without them. Preserve literal source text, quotations, code, command flags, URLs, email addresses, identifiers, citations, Markdown syntax, and structured fields exactly when the task requires them.';
