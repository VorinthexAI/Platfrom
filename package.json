{
  "name": "vorinthex-platform",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "web/landing-pages/*",
    "backend",
    "shared"
  ],
  "scripts": {
    "vorinthex:lint": "bun run --cwd web/landing-pages/vorinthex lint",
    "vorinthex:typecheck": "bun run --cwd web/landing-pages/vorinthex typecheck",
    "orbit:lint": "bun run --cwd web/landing-pages/orbit lint",
    "orbit:typecheck": "bun run --cwd web/landing-pages/orbit typecheck",
    "backend:check": "bun run --cwd backend check",
    "backend:test": "bun run --cwd backend test"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "@types/react": "^19",
    "@types/react-dom": "^19"
  }
}
