export const slideshowSystemPrompt = `Create one production-quality vertical TikTok slideshow slide.
Use a consistent campaign art direction across every slide in the set.
The output must be a premium 9:16 image with strong mobile readability.
Keep the same visual language, materials, color palette, lighting, camera distance, and composition rules across slides.
Reserve generous clean space for external overlay text and keep important subjects away from the top, bottom, left, and right edges.
Do not add watermarks, platform UI, fake app chrome, unreadable microtext, or random typography.
Never render text inside the image unless the slide brief explicitly overrides this.`;

export const slideshowNegativePrompt = `No watermark, no TikTok interface, no usernames, no QR codes, no clutter, no tiny unreadable text, no messy collage, no cartoon style, no neon cyberpunk overload, no cheap poster layout, no duplicated faces, no distorted hands, no malformed logos.`;
