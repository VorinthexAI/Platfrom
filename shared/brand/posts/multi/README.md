# Multi Post Layouts

Multi post layouts are slideshow/carousel templates.

Add one folder per format or aspect ratio.

Current layouts:

- `square/default-layout.html`
- `portrait/default-layout.html`

Each layout should use `../../layout.css` or the correct relative path to the
shared brand tokens in `brand/layout.css`.
